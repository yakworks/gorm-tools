type: object
properties:
  id:
    description: unique id
    type: integer
    format: int64
    example: 954
    readOnly: true
  version:
    description: version of the edit, incremented on each change
    type: integer
    format: int64
    example: 1
    readOnly: true
