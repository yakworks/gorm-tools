api:
  paths:
    rally:
      appParam:
        gridOptions:
          colModel:
            - { name: id, hidden: true }
            - { name: variable, width: 40, formatter: editActionLink }
            - { name: value, width: 40 }
          toolbar:
            leftButtons:
              create: { class: hidden }
        editForm:
          variable:
            disabled: true
          value: {}
