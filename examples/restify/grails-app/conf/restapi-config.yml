---
restApi:
  project:
    includes:
      default: ['*']
      list: ['id', 'num', 'name', 'billable']
      pickList: ['id', 'num', 'name']
  book:
    entityClass: restify.Book
    qSearch: ['num', 'name']
    includes:
      default: ['*']
      list: ['id', 'num', 'name', 'billable']
      pickList: ['id', 'num', 'name']
  org:
    entityClass: yakworks.taskify.domain.Org
    qSearch: ['num', 'name']
