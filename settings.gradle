rootProject.name = 'gorm-tools-root'

include "benchmarks"
include "test-app"

rootProject.children.each { project ->
  project.projectDir = new File(settingsDir, "examples/$project.name")
  //project.buildFileName = "${project.name}.gradle"
  //project.setProperty('examples',true)//so we can know not to deploy these
}

// Deployable projects
include 'plugin'
findProject(':plugin').with{
  name = 'gorm-tools'
  //buildFileName = "${name}.gradle"
}
